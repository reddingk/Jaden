<div class="home-body">
	<!-- Column 1 -->		
	<div class="chip-box col-sm-4" id="col-1">					
		<div class="chip-card" ng-repeat="chip in chips" ng-attr-id="{{chip.title.replace(' ','_')}}" ng-if="($index % 3 == 0)">						
			<a href="/{{chip.title.replace(' ','')|lowercase}}/{{$index}}" class="card-links">
				<div class="card-header">
					<div class="chip-header"><i class="fa" ng-class="{'fa-circle-o-notch fa-spin': chip.icon == '', '{{chip.icon}}': chip.icon != ''}"></i> {{chip.title}}</div>						
				</div>
			</a>
			<div class="chip-body">{{chip.title.replace(' ','_')}}</div>						
		</div>				
	</div>
	
	<!-- Column 2 -->
	<div class="chip-box col-sm-4" id="col-2">
		<div class="chip-card" ng-repeat="chip in chips" ng-attr-id="{{chip.title.replace(' ','_')}}" ng-if="(($index-1) % 3 == 0)">
			<a href="/{{chip.title.replace(' ','')|lowercase}}/{{$index}}" class="card-links">
				<div class="card-header">
					<div class="chip-header"><i class="fa" ng-class="{'fa-circle-o-notch': chip.icon == '', '{{chip.icon}}': chip.icon != ''}"></i> {{chip.title}}</div>						
				</div>
			</a>
			<div class="chip-body">{{chip.title.replace(' ','_')}}</div>
		</div>
	</div>			

	<!--Column 3 -->
	<div class="chip-box col-sm-4" id="col-3">
		<div class="chip-card" ng-repeat="chip in chips" ng-attr-id="{{chip.title.replace(' ','_')}}" ng-if="(($index-2) % 3 == 0)">
			<a href="/{{chip.title.replace(' ','')|lowercase}}/{{$index}}" class="card-links">
				<div class="card-header">
					<div class="chip-header"><i class="fa" ng-class="{'fa-circle-o-notch': chip.icon == '', '{{chip.icon}}': chip.icon != ''}"></i> {{chip.title}}</div>						
				</div>
			</a>
			<div class="chip-body">{{chip.title.replace(' ','_')}}</div>
		</div>
	</div>
	<div style="clear:both"></div>

</div>
